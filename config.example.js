// ⚠️ INSTRUCTIONS ⚠️
// 1. Rename this file to exactly: config.js
// 2. Paste your actual API keys below.
// 3. Keep config.js safe! It is already added to .gitignore so it won't upload to GitHub.

const CONFIG_KEYS = {
  // Groq (100% Free - Fast Llama 3)
  GROQ_API_KEY: "YOUR_GROQ_API_KEY_HERE",

  // Claude (Secondary/Paid)
  CLAUDE_API_KEY: "sk-ant-api03-...", 
};
